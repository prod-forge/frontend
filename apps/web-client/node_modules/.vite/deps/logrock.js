import { r as __toESM, t as __commonJSMin } from "./chunk-CYJPkc-J.js";
import { t as require_react } from "./react.js";
import { t as require_react_dom } from "./react-dom.js";
import { t as require_jsx_runtime } from "./react_jsx-runtime.js";
//#region ../../node_modules/logrock/lib/esm/types/index.mjs
var LoggerLevels = /* @__PURE__ */ function(LoggerLevels) {
	LoggerLevels["critical"] = "critical";
	LoggerLevels["debug"] = "debug";
	LoggerLevels["error"] = "error";
	LoggerLevels["info"] = "info";
	LoggerLevels["log"] = "log";
	LoggerLevels["warn"] = "warn";
	return LoggerLevels;
}({});
//#endregion
//#region ../../node_modules/logrock/lib/esm/helpers/limited-array.mjs
var DEFAULT_LIMIT = 10;
var LimitedArray = class {
	data = [];
	limit = 0;
	constructor(options) {
		this.limit = options && typeof options.limit === "number" ? Math.abs(options.limit) : DEFAULT_LIMIT;
	}
	add(item) {
		this.data.push(item);
		return this.trimToLimit();
	}
	at(index) {
		return this.data[index];
	}
	getData() {
		return this.data.map((item) => item);
	}
	getLength() {
		return this.data.length;
	}
	reset() {
		this.data = [];
	}
	setLimit(limit) {
		this.limit = limit && typeof limit === "number" ? Math.abs(limit) : this.limit;
		return this.trimToLimit();
	}
	splice(from, to) {
		this.data.splice(from, to);
	}
	trimToLimit() {
		const excess = this.data.length - this.limit;
		if (excess <= 0) return false;
		this.data.splice(0, excess);
		return true;
	}
};
//#endregion
//#region ../../node_modules/logrock/lib/esm/logger.mjs
var Logger = class {
	enabled = true;
	_count = 0;
	constructor() {
		this.stackCollection = new LimitedArray();
	}
	_handler(msg, level, important, ctx = "") {
		if (!this.enabled) return;
		let message;
		let payload = {};
		if (typeof msg === "object" && !(msg instanceof Error)) {
			message = msg.message || msg.msg || "";
			const rest = { ...msg };
			delete rest.message;
			delete rest.msg;
			payload = rest;
		} else if (msg instanceof Error) message = msg.message;
		else message = msg;
		if (typeof this.stdout === "function") this.stdout(level, message, ctx, important);
		this.stackCollection.add({
			ctx,
			level,
			message,
			payload
		});
		this._count += 1;
	}
	debug(message, ctx, important) {
		this._handler(message, LoggerLevels.debug, !!important, ctx);
	}
	error(message, ctx, important) {
		this._handler(message, LoggerLevels.error, !!important, ctx);
	}
	getCounter = () => this._count;
	getStackCollection = () => this.stackCollection;
	info(message, ctx, important) {
		this._handler(message, LoggerLevels.info, !!important, ctx);
	}
	log(message, ctx, important) {
		this._handler(message, LoggerLevels.log, !!important, ctx);
	}
	setUp(props) {
		if (typeof props.enabled === "boolean") this.enabled = Boolean(props.enabled);
		if (typeof props.stdout === "function") this.stdout = props.stdout;
	}
	warn(message, ctx, important) {
		this._handler(message, LoggerLevels.warn, !!important, ctx);
	}
};
var createLogger = () => new Logger();
var logger = createLogger();
//#endregion
//#region ../../node_modules/react/cjs/react-compiler-runtime.development.js
/**
* @license React
* react-compiler-runtime.development.js
*
* Copyright (c) Meta Platforms, Inc. and affiliates.
*
* This source code is licensed under the MIT license found in the
* LICENSE file in the root directory of this source tree.
*/
var require_react_compiler_runtime_development = /* @__PURE__ */ __commonJSMin(((exports) => {
	(function() {
		var ReactSharedInternals = require_react().__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE;
		exports.c = function(size) {
			var dispatcher = ReactSharedInternals.H;
			null === dispatcher && console.error("Invalid hook call. Hooks can only be called inside of the body of a function component. This could happen for one of the following reasons:\n1. You might have mismatching versions of React and the renderer (such as React DOM)\n2. You might be breaking the Rules of Hooks\n3. You might have more than one copy of React in the same app\nSee https://react.dev/link/invalid-hook-call for tips about how to debug and fix this problem.");
			return dispatcher.useMemoCache(size);
		};
	})();
}));
//#endregion
//#region ../../node_modules/react/compiler-runtime.js
/**
* Copyright (c) Meta Platforms, Inc. and affiliates.
*
* This source code is licensed under the MIT license found in the
* LICENSE file in the root directory of this source tree.
*/
var require_compiler_runtime = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_react_compiler_runtime_development();
}));
//#endregion
//#region ../../node_modules/logrock/lib/esm/helpers/error-helpers.mjs
var import_react = /* @__PURE__ */ __toESM(require_react(), 1);
var import_react_dom = /* @__PURE__ */ __toESM(require_react_dom(), 1);
var import_compiler_runtime = require_compiler_runtime();
var CRITICAL = "critical";
var isCritical = (type) => CRITICAL === type;
var createCritical = (trace, lineNumber) => ({
	ctx: "",
	level: LoggerLevels.critical,
	message: trace.message,
	payload: {
		line: lineNumber,
		stack: trace.stack ? trace.stack.split("\n") : []
	}
});
//#endregion
//#region ../../node_modules/logrock/lib/esm/components/bsod.mjs
var import_jsx_runtime = require_jsx_runtime();
var _h, _div;
var Bsod = (props) => {
	const $ = (0, import_compiler_runtime.c)(20);
	const { actions } = props.stackData;
	const lastAction = actions[actions.length - 1];
	const cError = lastAction?.level === LoggerLevels.critical ? lastAction.payload : void 0;
	let t0;
	let t1;
	if ($[0] === Symbol.for("react.memo_cache_sentinel")) {
		t0 = {
			background: "#00a",
			color: "#b3b3b3",
			fontFamily: "courier",
			fontSize: "14px",
			height: "100%",
			left: 0,
			position: "fixed",
			top: 0,
			width: "100%",
			zIndex: 1e4
		};
		t1 = {
			height: "100%",
			position: "relative",
			width: "100%"
		};
		$[0] = t0;
		$[1] = t1;
	} else {
		t0 = $[0];
		t1 = $[1];
	}
	let t2;
	if ($[2] !== props.onClose) {
		t2 = typeof props.onClose === "function" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
			onClick: props.onClose,
			style: {
				background: "#eee",
				border: 0,
				cursor: "pointer",
				fontSize: "24px",
				height: "30px",
				lineHeight: "25px",
				position: "absolute",
				right: "25px",
				textAlign: "center",
				top: "25px",
				WebkitAppearance: "none",
				width: "30px",
				zIndex: 1e3
			},
			type: "button",
			children: "X"
		});
		$[2] = props.onClose;
		$[3] = t2;
	} else t2 = $[3];
	let t3;
	if ($[4] !== cError) {
		t3 = cError && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("h2", {
			style: {
				background: "#b3b3b3",
				color: "#00a",
				display: "inline-block",
				fontFamily: "courier",
				fontSize: "18px",
				fontWeight: "bold",
				margin: "0 auto",
				padding: "0.25em 0.5em"
			},
			children: cError.stack[0] || cError.message || ""
		});
		$[4] = cError;
		$[5] = t3;
	} else t3 = $[5];
	let t4;
	if ($[6] === Symbol.for("react.memo_cache_sentinel")) {
		t4 = {
			fontSize: "1rem",
			margin: "2em",
			textAlign: "left"
		};
		$[6] = t4;
	} else t4 = $[6];
	let t5;
	if ($[7] !== cError) {
		t5 = cError && Array.isArray(cError.stack) && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("pre", {
			style: {
				background: "none",
				border: 0,
				color: "rgb(179, 179, 179)",
				font: "10px/13px Lucida Console, Monaco, monospace",
				margin: "0 0 10px"
			},
			children: ["$", cError.stack.join("\n")]
		});
		$[7] = cError;
		$[8] = t5;
	} else t5 = $[8];
	let t6;
	if ($[9] === Symbol.for("react.memo_cache_sentinel")) {
		t6 = _h || (_h = /* @__PURE__ */ (0, import_jsx_runtime.jsx)("h4", { children: "Actions:" }));
		$[9] = t6;
	} else t6 = $[9];
	let t7;
	if ($[10] !== actions || $[11] !== props.count) {
		t7 = actions.length > 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("ol", {
			reversed: true,
			start: props.count || actions.length - 1,
			style: {
				fontSize: "13px",
				listStyle: "list-item"
			},
			children: [...actions].reverse().filter(_temp).map(_temp2)
		}) : _div || (_div = /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: "Nothing actions" }));
		$[10] = actions;
		$[11] = props.count;
		$[12] = t7;
	} else t7 = $[12];
	let t8;
	if ($[13] !== t5 || $[14] !== t7) {
		t8 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			style: t4,
			children: [
				t5,
				t6,
				t7
			]
		});
		$[13] = t5;
		$[14] = t7;
		$[15] = t8;
	} else t8 = $[15];
	let t9;
	if ($[16] !== t2 || $[17] !== t3 || $[18] !== t8) {
		t9 = /* @__PURE__ */ (0, import_react_dom.createPortal)(/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			style: t0,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				style: t1,
				children: [
					t2,
					t3,
					t8
				]
			})
		}), document.body);
		$[16] = t2;
		$[17] = t3;
		$[18] = t8;
		$[19] = t9;
	} else t9 = $[19];
	return t9;
};
function _temp(action) {
	return !isCritical(action.level);
}
function _temp2(action_0, index) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("li", { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("strong", { children: [
		action_0.ctx ? `[${action_0.ctx}] ` : "",
		action_0.level,
		": ",
		action_0.message
	] }) }) }, index);
}
//#endregion
//#region ../../node_modules/logrock/lib/esm/helpers/ua-parser.mjs
var BROWSERS = [
	[/Edg\/(\d[\d.]*)/i, "Edge"],
	[/OPR\/(\d[\d.]*)/i, "Opera"],
	[/SamsungBrowser\/(\d[\d.]*)/i, "Samsung Internet"],
	[/Firefox\/(\d[\d.]*)/i, "Firefox"],
	[/Chrome\/(\d[\d.]*)/i, "Chrome"],
	[/Version\/(\d[\d.]*).*Safari/i, "Safari"],
	[/MSIE (\d[\d.]*)/i, "IE"],
	[/Trident\/.*rv:(\d[\d.]*)/i, "IE"]
];
var OS_PATTERNS = [
	[/Android (\d[\d.]*)/i, "Android"],
	[/iPhone OS ([\d_]+)/i, "iOS"],
	[/iPad.*CPU OS ([\d_]+)/i, "iPadOS"],
	[/Windows NT ([\d.]+)/i, "Windows"],
	[/Mac OS X ([\d_.]+)/i, "macOS"],
	[/Linux/i, "Linux"]
];
var parseUserAgent = (ua) => {
	let browser = "Unknown";
	let browserVersion = "";
	for (const [pattern, name] of BROWSERS) {
		const match = pattern.exec(ua);
		if (match) {
			browser = name;
			browserVersion = match[1] ?? "";
			break;
		}
	}
	let os = "Unknown";
	for (const [pattern, name] of OS_PATTERNS) {
		const match = pattern.exec(ua);
		if (match) {
			const version = match[1] ? match[1].replace(/_/g, ".") : "";
			os = version ? `${name} ${version}` : name;
			break;
		}
	}
	return {
		browser,
		browserVersion,
		os
	};
};
//#endregion
//#region ../../node_modules/logrock/lib/esm/helpers/collect-metadata.mjs
var hasWindow = () => typeof window !== "undefined";
var hasNavigator = () => typeof navigator !== "undefined";
var getViewport = () => hasWindow() ? `${window.innerWidth}x${window.innerHeight}` : "";
var getScreen = () => hasWindow() && window.screen ? `${window.screen.width}x${window.screen.height}` : "";
var getDevicePixelRatio = () => hasWindow() ? window.devicePixelRatio ?? 1 : 1;
var getLanguage = () => hasNavigator() ? navigator.language : "";
var getTimezone = () => {
	try {
		return Intl.DateTimeFormat().resolvedOptions().timeZone;
	} catch {
		return "";
	}
};
var getMobile = () => {
	if (!hasNavigator()) return false;
	return navigator.maxTouchPoints > 1 || /Mobi|Android|iPhone|iPad/i.test(navigator.userAgent);
};
var getUrl = () => hasWindow() ? window.location.pathname : "";
var getFullUrl = () => hasWindow() ? window.location.href : "";
var collectMetadata = () => ({
	...parseUserAgent(hasNavigator() ? navigator.userAgent : ""),
	devicePixelRatio: getDevicePixelRatio(),
	fullUrl: getFullUrl(),
	language: getLanguage(),
	mobile: getMobile(),
	screen: getScreen(),
	timezone: getTimezone(),
	url: getUrl(),
	viewport: getViewport()
});
//#endregion
//#region ../../node_modules/logrock/lib/esm/helpers/utils.mjs
var clone = (obj) => JSON.parse(JSON.stringify(obj));
//#endregion
//#region ../../node_modules/logrock/lib/esm/helpers/stack.mjs
var getStackData = (stack, stackCollection, props) => {
	stack.actions = stackCollection.getData();
	stack.timestamp = (/* @__PURE__ */ new Date()).toISOString();
	stack.metadata = collectMetadata();
	if (typeof props.onPrepareStack === "function") props.onPrepareStack(stack);
	return clone(stack);
};
var onCriticalError = (stack, stackCollection, props, trace, lineNumber) => {
	const entry = createCritical(trace, lineNumber);
	stackCollection.add(entry);
	if (typeof props.stdout === "function") props.stdout(entry.level, entry.message, entry.ctx, false);
	return getStackData(stack, stackCollection, props);
};
//#endregion
//#region ../../node_modules/logrock/lib/esm/logger-container.mjs
var LoggerContext = /* @__PURE__ */ (0, import_react.createContext)(null);
var isBackend = () => typeof window === "undefined";
var useLoggerApi = () => {
	const $ = (0, import_compiler_runtime.c)(3);
	const ctx = (0, import_react.useContext)(LoggerContext);
	let t0;
	if ($[0] !== ctx.getStackData || $[1] !== ctx.onError) {
		t0 = {
			getStackData: ctx.getStackData,
			triggerError: ctx.onError
		};
		$[0] = ctx.getStackData;
		$[1] = ctx.onError;
		$[2] = t0;
	} else t0 = $[2];
	return t0;
};
var LoggerContainer = ({ bsodComponent, children, enabled = true, env = "", limit = 25, onError: onErrorCallback, onPrepareStack, showBsod = true, stdout, traceId }) => {
	const [bsodVisible, setBsodVisible] = (0, import_react.useState)(false);
	const hasCriticalError = (0, import_react.useRef)(false);
	const stack = (0, import_react.useRef)({
		actions: logger.getStackCollection().data,
		env,
		metadata: {
			browser: "",
			browserVersion: "",
			devicePixelRatio: 1,
			fullUrl: "",
			language: "",
			mobile: false,
			os: "",
			screen: "",
			timezone: "",
			url: "",
			viewport: ""
		},
		timestamp: "",
		traceId: typeof traceId === "string" || typeof traceId === "number" ? traceId : void 0
	});
	(0, import_react.useEffect)(() => {
		logger.setUp({ enabled });
		logger.getStackCollection().setLimit(limit);
		if (typeof stdout === "function") logger.setUp({ stdout });
	}, [
		enabled,
		limit,
		stdout
	]);
	const buildProps = (0, import_react.useCallback)(() => ({
		...typeof onPrepareStack === "function" ? { onPrepareStack } : {},
		...typeof stdout === "function" ? { stdout } : {}
	}), [onPrepareStack, stdout]);
	const getStackDataFn = (0, import_react.useCallback)(() => getStackData(stack.current, logger.getStackCollection(), buildProps()), [buildProps]);
	const handleError = (0, import_react.useCallback)((stackData) => {
		if (typeof onErrorCallback === "function") onErrorCallback(stackData);
	}, [onErrorCallback]);
	const closeBsod = (0, import_react.useCallback)(() => {
		setBsodVisible(false);
	}, []);
	(0, import_react.useEffect)(() => {
		if (!enabled || isBackend()) return;
		const triggerCritical = (error, lineno) => {
			if (hasCriticalError.current) return;
			hasCriticalError.current = true;
			handleError(onCriticalError(stack.current, logger.getStackCollection(), buildProps(), error, lineno));
			setBsodVisible(true);
		};
		const onError = (e) => {
			e.preventDefault();
			triggerCritical(e.error, e.lineno);
		};
		const onUnhandledRejection = (e_0) => {
			e_0.preventDefault();
			triggerCritical(e_0.reason instanceof Error ? e_0.reason : new Error(String(e_0.reason ?? "Unhandled promise rejection")), 0);
		};
		window.addEventListener("error", onError);
		window.addEventListener("unhandledrejection", onUnhandledRejection);
		return () => {
			window.removeEventListener("error", onError);
			window.removeEventListener("unhandledrejection", onUnhandledRejection);
		};
	}, [
		enabled,
		buildProps,
		handleError
	]);
	const contextValue = (0, import_react.useMemo)(() => ({
		getStackData: getStackDataFn,
		onError: handleError
	}), [getStackDataFn, handleError]);
	const BsodToRender = bsodComponent ?? Bsod;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(LoggerContext.Provider, {
		value: contextValue,
		children: [children, showBsod && bsodVisible && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(BsodToRender, {
			count: logger.getCounter(),
			onClose: closeBsod,
			stackData: getStackDataFn()
		})]
	});
};
//#endregion
//#region ../../node_modules/logrock/lib/esm/index.mjs
var esm_default = logger;
//#endregion
export { LoggerContainer, LoggerLevels, esm_default as default, logger, useLoggerApi };

//# sourceMappingURL=logrock.js.map